paths:
  migrations: db/migrations
  seeds: db/seeds 

environments:
  default_migration_table: phinxlog ##создание таблицы в базе данных для хранения информации о выполненных миграциях
  default_environment: development ##указываем что по умолчанию для подключения будут использованы параметры прописанные в development 
  development:
    adapter: mysql
    host: 127.0.0.1
    name: timetable
    user: root
    pass: ""
    port: 3306
    charset: utf8